# RoboCloud GPU Agent —— AMD 机器上的 .env 模板（隧道接入版）
# 复制为 .env 并填入实际值后启动 Agent。对应设计文档第 5 节。

# ================= 后端对接 =================
# Agent 向后端注册/心跳的地址（后端可达）。通常经 volc 反代或内网直达后端。
SCHEDULER_API_BASE_URL=http://<后端可达地址>:6150/api/v1
AGENT_NODE_NAME=amd-gpu-01
AGENT_VERSION=1.0.0

# ================= 控制面（后端 -> Agent）=================
# AGENT_IP 是后端用来拼 http://{ip}:{api_port} 回连 Agent 的地址：填 volc 上后端可达的 IP。
# 后端流量 -> volc:6152 -> autossh 隧道(31081) -> AMD 本机 6152。
AGENT_IP=<volc后端可达IP>
AGENT_PORT=6152
# 只需监听本地回环，隧道落在 localhost；不要暴露 0.0.0.0。
AGENT_LISTEN_HOST=127.0.0.1

# GPU 资源（按 AMD 机器实际填）
AGENT_GPU_TOTAL=1
AGENT_GPU_SLOTS_PER_GPU=1

# ================= 数据面（浏览器 -> Agent，子路由）=================
# 浏览器直传数据集的公网基址 = RoboCloud 域名 + /amd-agent 子路由。
AGENT_PUBLIC_BASE_URL=https://<RoboCloud域名>/amd-agent
AGENT_UPLOAD_ENABLED=true
# 同源，无需 CORS 白名单（留空）。
AGENT_UPLOAD_ALLOWED_ORIGINS=

# ================= 推理面（机器人 -> policy_server）=================
# 本地直连方案：机器人在本机开 ssh -L 6153:127.0.0.1:6153，连 localhost。
AGENT_INFERENCE_PUBLIC_HOST=127.0.0.1
AGENT_INFERENCE_PORT_START=6153
AGENT_INFERENCE_PORT_RANGE=50
# 若改为推理走 volc 中转（客户端零秘密，多一跳延迟），把上面改成：
# AGENT_INFERENCE_PUBLIC_HOST=<volc公网IP>
